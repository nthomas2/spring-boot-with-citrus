spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/appuser
    username: appuser
    password: password
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop

logging:
  level:
    root: INFO
